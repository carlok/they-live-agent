version: '3.8'
services:
  field-agent-web:
    build: .
    ports:
      - "443:443"
      - "80:80"
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt:ro
    environment:
      - USE_EXTERNAL_CERT=true
